# No third-party dependencies - this framework is stdlib-only and runs on
# Python 3.10+ with no `pip install`. This file is intentionally empty so CI/tooling
# that expects a requirements.txt still works.
